# Netlify builds straight from the GitHub repo. Environment variables (demo gate,
# site URL, analytics) are set in the Netlify UI, not here, because this file is
# shared by the demo and the eventual production site.
[build]
  command = "npm run build"

[[plugins]]
  package = "@netlify/plugin-nextjs"

[build.environment]
  NODE_VERSION = "22"
